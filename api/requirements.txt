fastapi>=0.115
uvicorn[standard]>=0.32
strawberry-graphql[fastapi]>=0.250
sqlalchemy[asyncio]>=2.0.36
asyncpg>=0.30
alembic>=1.14
pydantic-settings>=2.6
# Auth0 access-token (RS256/JWKS) and DPoP proof validation. The [crypto] extra
# pulls in `cryptography`, which also covers EC/RSA DPoP-proof verification and
# RFC 7638 JWK thumbprints, so no further dependency is needed.
pyjwt[crypto]>=2.10
