fastapi>=0.115
uvicorn[standard]>=0.32
strawberry-graphql[fastapi]>=0.250
sqlalchemy[asyncio]>=2.0.36
asyncpg>=0.30
alembic>=1.14
pydantic-settings>=2.6
# Auth0 access-token validation (RS256/JWKS). The [crypto] extra pulls in
# `cryptography`, required for RS256 signature verification.
pyjwt[crypto]>=2.10
